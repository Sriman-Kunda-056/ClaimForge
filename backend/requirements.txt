# --- core ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9
python-dotenv>=1.0
pytest>=8.0

# --- AI / LLM ---
openai>=1.30          # OpenAI-compatible client (Groq / Together / Ollama)

# --- database ---
libsql-experimental>=0.0.34   # Turso cloud SQLite (falls back to local sqlite3 if env vars not set)

# --- document extraction ---
pymupdf>=1.24         # PDF text layer extraction for the /extract endpoint
